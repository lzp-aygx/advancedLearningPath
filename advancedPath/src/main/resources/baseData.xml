<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="advancedPath.dao.BaseDataDao">
    <!-- Page样例 -->
    <select id="findPaperById" resultType="advancedPath.entity.Paper" parameterType="int">
        <![CDATA[

            select * from tblpaper WHERE fid =#{id}

        ]]>
    </select>
    <select id="findPapers" resultType="advancedPath.entity.Paper">
        <![CDATA[

            select * from tblpaper

        ]]>
    </select>

    <!-- EmailLog -->
    <select id="findEmailLogById" resultType="advancedPath.entity.EmailLog">
        <![CDATA[

            select * from tblEmailLog WHERE CompanyId =#{0} and ComplaintNo = #{1}

        ]]>
    </select>
    <select id="findEmailLogs" resultType="advancedPath.entity.EmailLog">
        <![CDATA[

            select * from tblEmailLog where CompanyId = #{0}

        ]]>
    </select>
    <select id="findAnswers" resultType="advancedPath.entity.Answer">
        <![CDATA[

            select * from tblAnswer where CompanyId = #{0}

        ]]>
    </select>
</mapper>
